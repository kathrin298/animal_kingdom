<div class="container">
  <%= simple_form_for [@booking, @review] do |f| %>
    <%= f.input :title %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.button :submit %>
  <% end %>
</div>
